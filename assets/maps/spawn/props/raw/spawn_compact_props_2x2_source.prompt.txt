Use the first visible image immediately above as the exact art-style reference. Use the second visible image only as an invisible 2x2 slot, spacing, centering, and safe-padding guide; do not copy its boxes, borders, center marks, lines, colors, or background. Create exactly one 2 rows by 2 columns compact environmental prop sheet for the same 2D Godot wilderness map. Row-major cell contents: 1) compact cluster of three rounded gray field rocks, 2) dense oval patch of tall concealing green grass with readable individual blades and an open lower center footprint, 3) compact leafy berry bush with a few small red berries, 4) primitive unlit stone-ring campfire with stacked short brown logs. Match the map's clean hand-painted, pixel-inspired rendering, bright luminous natural palette, crisp readable silhouettes, soft controlled highlights, and approximately 40-degree 3/4 top-down RPG perspective. Keep each prop fully inside the central 50% to 60% of its own equal square cell with generous gutters on all sides. No prop, blade, leaf, berry, stone, log, highlight, particle, smoke, flame, or shadow may touch or cross a cell edge. Background must be exactly solid flat #FF00FF magenta across the entire sheet and every cell, with no gradients, texture, floor plane, grass ground, cast shadows, separators, borders, grid lines, labels, numbers, text, UI, watermark, building, house, fence, character, or extra object. Exactly four props, one per cell.
