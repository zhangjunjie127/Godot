Use the visible dressed wilderness map immediately above as the exact style reference. Create one single reusable large leafy deciduous tree prop for this 2D Godot game. Match the map's clean hand-painted, pixel-inspired rendering, luminous fresh green palette, crisp readable silhouette, soft controlled highlights, and approximately 40-degree 3/4 top-down RPG perspective. The tree should have a rounded layered canopy, rich green leaves with yellow-green sunlit tips, a short readable brown trunk and visible root base, matching the trees around the dressed map edges. Full tree visible and centered, generous margin on every side, no clipping. The trunk base is the gameplay anchor and should be clearly readable for collision and y-sorting. Background must be exactly solid flat #FF00FF magenta everywhere, with no gradient, texture, scenery, floor plane, grass, cast shadow, border, grid, text, UI, label, watermark, building, house, fence, character, or extra object. One tree only.
